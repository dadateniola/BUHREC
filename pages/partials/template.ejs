<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate</title>

</head>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');
    @import url('https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap');

    * {
        margin: 0;
        padding: 0;
        color: white;
        box-sizing: border-box;

        font-family: "Inter", sans-serif;
        font-weight: 400;
        text-align: center;
    }

    :root {
        --errie-black: #171717;
        --light: #FFFFFF;
        --jet: #2C2C2C;
        --wisteria: #D0AAE7;
        --malachite: #4DE265;
        --ever-green: #567568;
    }

    body {
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        gap: 20px;
        background-color: var(--errie-black);
    }

    .content {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .content-head h1 {
        color: var(--wisteria);
        text-transform: uppercase;
        font-size: 40px;
        font-weight: 800;
    }

    .heading {
        font-size: 30px;
        font-weight: 600;
    }

    img {
        width: 30%;
        height: 40%;

    }

    .sign {
        position: absolute;
        bottom: 40px;
        right: 40px;
        padding: 30px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }

</style>

<body onload="displayCurrentDate()">
    <div class="content">
        <div class="content-head">
            <h1>buhrec</h1>
        </div>
        <h1 class="heading">Certificate of Completion</h1>
        <p>This is to certify that <%= owner %> has successfully completed the project "<%= task %>".</p>
        <p>Reviewed by: <%= reviewer %>
        </p>
        <div class="sign">
            <i>Supervisor: Dr Maitanmi</i>
            <p id="currentDate"> </p>
        </div>
    </div>
    <script>
        function displayCurrentDate() {
            // Get the current date
            const currentDate = new Date();

            // Get day, month, and year components
            const day = String(currentDate.getDate()).padStart(2, '0');
            const month = String(currentDate.getMonth() + 1).padStart(2, '0'); // Months are zero-based
            const year = currentDate.getFullYear();

            // Format the date as dd/mm/yyyy
            const formattedDate = `${day}/${month}/${year}`;

            // Set the formatted date as the text content of the <p> tag
            document.getElementById('currentDate').textContent = formattedDate;
        }
    </script>
</body>